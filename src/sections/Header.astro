---
import Navbar from "../components/navbar.astro";
---

<header class="flex-row">
  <a href="/"><img src="/img/logo.jpg" alt="img-logo" /></a>

  <!-- <Navbar /> -->
  <Navbar />
  <!-- <div class="icon-cart">
    <i class="bi bi-cart flex-row"></i>
    <span>0</span>
  </div> -->
</header>

<style>
  header {
    align-items: center;
    justify-content: space-between;
    position: sticky;
    top: 10px;
    background: rgba(255, 255, 255, 0.95);
    box-shadow: 0 0 14px rgba(0, 0, 0, 0.35);
    border-radius: 20px;
    width: 95%;
    height: 100px;
    margin: 10px auto;
    padding: 12px;
    transition: var(--transition);
    z-index: 100;
  }

  header a {
    height: 100%;
    margin-right: auto;
    display: flex;
  }

  header img {
    height: 100%;
    width: auto;
    border-radius: 8px;
    /* border: 1px solid var(--clr-gray); */
  }

  /* ICONO CARRITO (global porque lo manipulamos con JS) */
  .icon-cart {
    background-color: #fff;
    border-radius: 12px;
    padding: 11.2px;
    cursor: pointer;
    position: relative;
    margin-left: 7px;
    border: solid 1px var(--clr-gray);
  }

  .icon-cart:hover {
    background-color: var(--clr-gray);
  }

  .icon-cart .bi-cart {
    font-size: 20px;
    color: var(--clr-dark);
  }
  .icon-cart span {
    position: absolute;
    color: #fff;
    background-color: var(--clr-dark);
    width: 16px;
    height: 16px;
    display: flex;
    justify-content: center;
    align-items: center;
    border-radius: 50%;
    font-weight: 200;
    top: 4px;
    right: 4px;
    font-size: 10px;
    padding-top: 1px;
  }

  @media (min-width: 700px) {
    header {
      background: rgba(255, 255, 255, 0.6);
      backdrop-filter: blur(5px);
      width: 98%;
    }

    header:hover {
      background-color: var(--clr-white);
    }
  }
</style>
